{
  "name": "agent-step-gate",
  "version": "0.3.0",
  "description": "CLI + Skill-based Step Ledger & Key Gate for long-running multi-agent orchestration",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "step-gate": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "cli": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["agent", "orchestration", "mcp", "step-gate", "dag", "multi-agent"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^11.0.0",
    "zod": "^4.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
